# Anable Hybrid TTS Debug
youre able to debug in the app

## Anable debugging
Go to `_DGS-DATA` > `Hybrid-TTS` > `debug`, then edit the `debug.txt` file with `true`!
> **debug.txt don't exsist by you?**     
> Create `debug.txt` in the `debug`-folder